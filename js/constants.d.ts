export declare var EXEC_URL: string;
export declare var EXEC_SHORTCUT: string;
/**
 * Responses of a dispatcher
 */
export declare const enum ResponseCode {
    NOMATCH = 0,
    EXEC = 1,
    QUERY = 2
}
